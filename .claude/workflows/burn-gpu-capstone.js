export const meta = {
  name: 'burn-gpu-capstone',
  description: 'Capstone: prove a REAL ML workload (Burn/CubeCL tensor on a CUDA GPU) end-to-end through the facade — #[modal_rust::function(gpu="T4")] on examples/burn-add, run/deploy+call on Modal returning a real GPU tensor result. Requires a CUDA-devel base image (cudart/nvrtc) + rust toolchain provisioning in the image (a new facade/SDK capability), reusing the proven prototype recipe.',
  phases: [
    { title: 'Design', detail: 'CUDA-devel base + rust-install image recipe (from the proven gpu_app.py) + burn-add macro/gpu + facade extension -> spec' },
    { title: 'Implement', detail: 'custom base + rust provisioning in ImageSpec/RemoteConfig/DeployConfig; burn-add -> #[function(gpu)]; offline gates green (HARD GATE)' },
    { title: 'Live', detail: 'deploy+call (and/or remote) burn_add on a CUDA GPU -> real Burn tensor result; iterate CUDA base version; retry; be patient (heavy build)' },
    { title: 'Review', detail: 'parallel: CUDA image recipe matches the proven prototype + GPU tier + build boundary; hygiene (burn-add stays out of default-members)' },
  ],
}

const ROOT = '/Users/nicolas/devel/modal-rust'

const SHARED = [
  'You are building the BURN/GPU CAPSTONE of modal-rust (repo root: ' + ROOT + '; git on main).',
  '',
  '## Where we are',
  'The full product is proven live for the trivial `add` (CPU) and a driver-only cudarc `vector_add` (GPU/T4):',
  '`.local()`/`.remote()`/`deploy`/`call` via our own SDK (no modal-rs) + facade; `#[modal_rust::function(gpu="T4")]`',
  'decorator config flows into `FunctionCreate.resources.gpu_config` (P4); the CLI is programmatic (P9); the image',
  'uses add_python (matches the official client) on a `rust:1-slim` base; the upload is cargo-scoped. What is NOT yet',
  'proven: a REAL, HEAVY ML workload — a Burn/CubeCL tensor op that needs the **CUDA toolkit** (cudart/nvrtc), not',
  'just the driver. This capstone proves it works under real weight, through the facade.',
  '',
  '## What this builds',
  '1. **A CUDA-devel image capability** (new): the run/deploy image must be a `nvidia/cuda:<ver>-devel` base (carries',
  '   cudart + nvrtc that cubecl-cuda links at build AND runtime) with the **rust toolchain installed** (the CUDA base',
  '   has no rust) + python (add_python or apt) + the wrapper. Today the image is hardcoded to `from_registry(rust:1-slim)`',
  '   + add_python. Extend `ImageSpec`/`RemoteConfig`/`DeployConfig` so the base image is configurable AND a rust-install',
  '   step is added when the base lacks rust (mirror the PROVEN prototype recipe). This is a Tier-1 image (boundaries.md §9).',
  '2. **burn-add through the facade**: `examples/burn-add` (CUDA-only Burn/CubeCL tensor add, already proven via the',
  '   Python shim in prototype M13) carries `#[modal_rust::function(gpu="...")]`; invoke it through `App` (run and/or',
  '   deploy+call) → a real Burn tensor result computed on a CUDA GPU on Modal.',
  '',
  '## The PROVEN recipe to port (do NOT reinvent)',
  'workpads/gpu-compute/gpu_app.py (the `burn_add` local_entrypoint) is the VALIDATED Tier-1 recipe from M13: it uses a',
  '`nvidia/cuda:<ver>-devel` base, installs the rust toolchain, adds python, builds `-p example-burn-add --bin',
  'modal_runner`, runs on a GPU. READ IT for the exact base image version, the rust-install commands, the env',
  '(CUDA paths, LD_LIBRARY_PATH), and the GPU type that worked. Replicate that image programmatically.',
  '',
  '## CUDA version is load-bearing — reconcile it',
  'examples/burn-add/Cargo.toml notes that `cubecl-cuda` references CUDA symbols only present with a matching CUDA',
  'toolkit (the comment mentions CUDA-13). The prototype gpu_app.py used a specific `nvidia/cuda:<ver>-devel`. READ',
  'BOTH and pick the CUDA-devel base version that matches the CURRENT `cubecl-cuda`/`cudarc` versions in',
  'examples/burn-add/Cargo.toml. If the live build fails on missing CUDA symbols (e.g. `undefined reference to`',
  'cuda13-only symbols), BUMP the CUDA base version (13.x-devel) and retry — this is expected iteration, not a design',
  'failure. The GPU itself (T4 supports CUDA 12/13 runtime) is fine; the BUILD-time toolkit version is what matters.',
  '',
  '## Build cost / strategy',
  'This is the HEAVIEST build in the project (CUDA image pull + rust install + cubecl/burn compile = MANY minutes).',
  'Prefer the **DEPLOY path** for the primary live proof — build ONCE at image-build time (Modal caches the image by',
  'hash; subsequent calls are fast), rather than the run path which rebuilds in-body every invoke. (Do the run path too',
  'only if time/robustness allow.) Use a CHEAP GPU (T4) and a STABLE deploy app name (re-deploy replaces in place).',
  '',
  '## Ground-truth references (READ)',
  '- workpads/gpu-compute/gpu_app.py (the proven burn_add Tier-1 image recipe) + workpads/gpu-compute/knowledge.md',
  '  + workpads/architecture/boundaries.md §9 (GPU tiering: Tier 1 = nvrtc/cudart for Burn/cubecl).',
  '- examples/burn-add/{src/lib.rs (the burn_add fn + modal_registry + the libnvrtc/libcudart self-check), Cargo.toml',
  '  (cubecl-cuda/cudarc/burn versions — the CUDA toolkit version driver), src/bin/modal_runner.rs}.',
  '- crates/modal-rust-sdk/src/ops/image.rs (ImageSpec: from_registry/add_python/with_apt/run_commands/context_mount/',
  '  base_image) + crates/modal-rust/src/{remote.rs (RemoteConfig: base_image, the run image build), deploy.rs',
  '  (DeployConfig, the two-layer deploy image)}. See how P4 added gpu + how the add_python image is assembled.',
  '- crates/modal-rust-macros/src/lib.rs (the `#[modal_rust::function(gpu=...)]` macro) + how examples/cuda-vector-add',
  '  was converted to the macro+gpu in P4 (the pattern to mirror for burn-add).',
  '- Root Cargo.toml: example-burn-add is a MEMBER but EXCLUDED from default-members (CUDA-only) — KEEP it excluded.',
  '- workpads/shim-backend/knowledge.md, TASKS.md. Do NOT touch README.md or examples/orchestrate.',
  '',
  '## FROZEN invariants — do NOT change',
  '- The runner protocol / HandlerFn / typed! / Registry dispatch; the run-vs-deploy build boundary (run = build-in-body;',
  '  deploy = build-at-image-time, runtime execs prebuilt binary); retry_transient on all RPCs; ephemeral-run vs',
  '  persistent-deploy; the cargo-scoped upload; the add_python path for the rust:slim default (the CUDA base is an',
  '  ADDITIONAL configurable option, not a replacement of the default).',
  '- example-burn-add stays OUT of default-members (it cannot compile without CUDA). Offline gates verify the facade/SDK',
  '  EXTENSION compiles on default-members; burn-add itself only builds on Modal (the live proof).',
  '',
  '## Verification rules (WORKING.md)',
  '- Offline gates on default-members: cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ;',
  '  cargo test — all green (these do NOT build burn-add). Keep no-CUDA CI green.',
  '- Modal flakiness => RETRY. DRIVE the live proof to a terminal result; be PATIENT (the CUDA+burn build takes many',
  '  minutes). Use a stable deploy name; clean up ephemeral apps. T4 (cheap).',
  '',
  '## How to return',
  'End with: "RESULT: <STATUS> — <one-line>". Build phases STATUS in {BUILD_GREEN, BUILD_FAILED} + exact cargo output.',
  'Live: the real Burn tensor result + the CUDA base version that worked + GPU evidence, or the precise error after retries.',
].join('\n')

phase('Design')
const design = await parallel([
  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Design / CUDA-devel + rust-install image recipe). Read workpads/gpu-compute/gpu_app.py (the proven',
    'burn_add Tier-1 recipe) + workpads/gpu-compute/knowledge.md + examples/burn-add/Cargo.toml (cubecl-cuda/cudarc',
    'versions) + crates/modal-rust-sdk/src/ops/image.rs + crates/modal-rust/src/{remote.rs,deploy.rs}. Produce a PRECISE',
    'spec for:',
    '- The CUDA-devel image recipe: which `nvidia/cuda:<ver>-devel` base matches the current cubecl-cuda/cudarc versions',
    '  (reconcile the "CUDA-13 symbols" note vs the prototype version — state the version to use + why); the rust-toolchain',
    '  install commands (rustup, as in gpu_app.py); python provisioning on the CUDA base (add_python vs apt); any',
    '  env (CUDA paths / LD_LIBRARY_PATH / NVRTC) needed at build + runtime.',
    '- The minimal ADDITIVE extension to ImageSpec/RemoteConfig/DeployConfig so the base image is configurable AND a',
    '  rust-install step is emitted when the base is not already a rust image (the default rust:1-slim + add_python path',
    '  stays unchanged). How a user/example selects the CUDA base (config field / a knob keyed off the gpu/tier).',
    'Cite file:line + the exact base version + commands. RESULT: SPEC_DONE — CUDA image + rust-install recipe spec',
  ].join('\n'), { phase: 'Design', label: 'design:cuda-image' }),

  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Design / burn-add via the facade). Read examples/burn-add/{src/lib.rs,Cargo.toml,src/bin/modal_runner.rs},',
    'crates/modal-rust-macros/src/lib.rs, and how examples/cuda-vector-add was converted to `#[modal_rust::function(gpu=)]`',
    '+ from_inventory in P4 (git log / the current cuda-vector-add). Produce a PRECISE spec for:',
    '- Converting burn-add to carry `#[modal_rust::function(gpu="T4")]` on its `burn_add` entrypoint (+ from_inventory',
    '  bin) so the decorator GPU config + the function are discoverable through the facade/CLI — mirroring the P4',
    '  cuda-vector-add conversion. Keep burn-add CUDA-only + out of default-members.',
    '- How to invoke burn_add through the facade: a live test/example (behind #[ignore] + live feature) that does',
    '  deploy+call (primary, build-once) and/or .remote() with the CUDA-devel image config, returning the real Burn',
    '  tensor result. What input/output types burn_add uses; what a successful result looks like (the tensor sum / shape).',
    '- The package/closure: the cargo-scoped upload must include example-burn-add (the target) + its workspace-member',
    '  deps; confirm `cargo build -p example-burn-add --bin modal_runner` is the build command on the CUDA image.',
    'Cite file:line. RESULT: SPEC_DONE — burn-add-via-facade spec',
  ].join('\n'), { phase: 'Design', label: 'design:burn-facade' }),
])

const spec = await agent(SHARED + '\n\n' + [
  'YOUR TASK (Synthesize). Merge the two notes into ONE build-ready spec and WRITE it to',
  ROOT + '/workpads/shim-backend/burn-capstone-spec.md (overwrite if present): the CUDA-devel base + rust-install',
  'image extension (exact base version, commands, env, the additive ImageSpec/RemoteConfig/DeployConfig fields), and',
  'the burn-add macro/gpu conversion + the facade deploy+call/remote invocation. Note which files change. Preserve the',
  'frozen invariants + burn-add staying out of default-members. Resolve the CUDA-version question decisively. Keep tight.',
  '',
  '=== CUDA IMAGE + RUST-INSTALL NOTE ===',
  (design[0] || '(missing)'),
  '',
  '=== BURN-ADD VIA FACADE NOTE ===',
  (design[1] || '(missing)'),
  '',
  'RESULT: SPEC_DONE — wrote burn-capstone-spec.md',
].join('\n'), { phase: 'Design', label: 'design:synthesize' })

phase('Implement')
const impl = await agent(SHARED + '\n\n' + [
  'RESUME NOTE: a prior run died on a TRANSIENT infra socket error before completing — the Design phase + the spec',
  'are complete; implement them now. Read the CURRENT crate state first (a prior attempt may have left partial,',
  'uncompiled edits — finish/fix them to green). If the spawn blips again, that is infra, not a real failure.',
  'The spec is at ' + ROOT + '/workpads/shim-backend/burn-capstone-spec.md — READ IT FIRST.',
  '',
  'YOUR TASK (Implement the capstone capability — HARD GATE on offline gates). Per the spec:',
  '1. Extend ImageSpec/RemoteConfig/DeployConfig (ADDITIVELY) to support a configurable CUDA-devel base + a',
  '   rust-toolchain install step (+ the needed env), so the image can be `nvidia/cuda:<ver>-devel` + rust + python +',
  '   wrapper. The default rust:1-slim + add_python path MUST stay unchanged (add a test).',
  '2. Convert examples/burn-add to `#[modal_rust::function(gpu="T4")]` + from_inventory (mirror the P4 cuda-vector-add',
  '   conversion), so burn_add is discoverable through the facade with its GPU config. Keep burn-add CUDA-only + out of',
  '   default-members.',
  '3. Add the live deploy+call (and/or remote) test for burn_add (behind #[ignore] + the live feature) using the CUDA',
  '   image config — it will only RUN live (next phase), but must COMPILE under the live feature.',
  'Do NOT change the frozen invariants or the build boundary. Do NOT touch README.md / examples/orchestrate.',
  'VERIFY (offline hard, default-members — does NOT build burn-add): cargo fmt --check ; cargo clippy --all-targets --',
  '-D warnings ; cargo build ; cargo test — all green. Also confirm the live test compiles under --features live.',
  'Paste exact output.',
  'RESULT: BUILD_GREEN — CUDA-base+rust-install image extension + burn-add macro/gpu + live test compile green',
  '   (or BUILD_FAILED — <reason>)',
].join('\n'), { phase: 'Implement', label: 'capstone-impl' })

const implGreen = /RESULT:\s*BUILD_GREEN/i.test(impl || '')
let live = null
if (!implGreen) {
  log('Implement HARD GATE not green — the CUDA image extension / burn-add conversion did not compile. Skipping Live; Review documents the blocker.')
} else {
  phase('Live')
  live = await agent(SHARED + '\n\n' + [
    'The CUDA-image capability + burn-add macro/gpu are implemented and the offline gates are green. Run the LIVE',
    'CAPSTONE and DRIVE IT TO A TERMINAL RESULT yourself (do NOT punt to a background monitor). This is the heaviest',
    'build in the project — BE PATIENT (CUDA image pull + rust install + cubecl/burn compile = many minutes).',
    '',
    'YOUR TASK (LIVE — a real Burn/CUDA tensor op via the facade). Prove burn_add runs on a real CUDA GPU through the',
    'facade. PRIMARY: the DEPLOY path (build ONCE at image-build time on the `nvidia/cuda:<ver>-devel` image, Modal',
    'caches it) + call → the real Burn tensor result on a T4. Steps: build the CUDA-devel + rust image, COPY + cargo',
    'build -p example-burn-add at image-build time, deploy (stable name), then call burn_add → the tensor result.',
    'If the IMAGE BUILD fails on missing CUDA symbols, BUMP the CUDA base version (e.g. 12.x -> 13.x-devel) and retry',
    '(expected iteration). Modal flakiness => RETRY (retry_transient + image caching help; the FIRST build is slow,',
    'later calls are fast). If a real code bug surfaces, make the MINIMAL fix + re-verify offline gates.',
    'Capture: the real Burn tensor result (value/shape); the CUDA base version that WORKED; GPU evidence (the burn_add',
    'self-check / device name); proof cargo ran at image-build time (deploy) not at call; gpu_config=the GPU on',
    'FunctionCreate. (Attempt the run path too only if the deploy path succeeded and time allows.)',
    'RESULT: BUILD_GREEN — live Burn tensor op ran on a CUDA GPU via the facade (deploy+call), CUDA base <ver>',
    '   (or BUILD_FAILED — <real bug>   or INFRA_BLOCKED — <transient/build-time after N retries, with the exact error>)',
  ].join('\n'), { phase: 'Live', label: 'live-burn' })
}

phase('Review')
const reviews = await parallel([
  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / CUDA recipe + GPU tier + build boundary). Verify against the code + workpads/gpu-compute/',
    'gpu_app.py + boundaries.md §9:',
    '- The CUDA-devel base + rust-install image recipe matches the PROVEN prototype (correct base version for the current',
    '  cubecl-cuda, rust install, env); it is an ADDITIVE option — the default rust:1-slim + add_python path is unchanged',
    '  (point to the test). burn_add carries the decorator gpu config and is invoked through the facade.',
    '- The build boundary: for deploy, cargo (incl. the heavy burn build) runs at IMAGE-BUILD time and the deployed body',
    '  execs only the prebuilt binary (no cargo at call); for run, in-body. Quote the relevant code.',
    '- example-burn-add stays CUDA-only + out of default-members; the cargo-scoped upload includes it for the build.',
    'RESULT: PASS — CUDA recipe matches prototype, boundary intact, default path unchanged  (or FAIL — <what is wrong>)',
  ].join('\n'), { phase: 'Review', label: 'review:cuda-recipe' }),

  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / hygiene — RUN the gates). From ' + ROOT + ' report exact output + exit status:',
    '- cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test  (default-members).',
    'Confirm example-burn-add is STILL excluded from default-members (no-CUDA CI green — offline gates do not build it);',
    'cuda-vector-add still builds without CUDA; live tests #[ignore]+live gated; README.md + examples/orchestrate',
    'untouched; no hand-written file grossly exceeds ~500 LOC. Report failures verbatim.',
    'RESULT: PASS — gates green, burn-add still excluded  (or FAIL — <exact failing command + output>)',
  ].join('\n'), { phase: 'Review', label: 'review:hygiene' }),
])

return { impl_green: implGreen, impl, live, reviews }
