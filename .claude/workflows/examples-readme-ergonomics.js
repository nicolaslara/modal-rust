export const meta = {
  name: 'examples-readme-ergonomics',
  description: 'Make the MACRO example read like clean, Python-like user code: promote the ergonomic auto-I/O form to be THE headline `add` (a ~3-line `#[function] fn add(a,b)->Result<i64>`), renaming the awkward `add_plain`, and strip the macro example down from a 442-line proof/reference file to what a real user writes. Relocate the live-proof + config-demo bits out of the showcased lib.rs. Rewire the few consumers (live_auto_io, orchestrate, live_secrets_volumes) + the README so `add` (not `add_plain`) is the ergonomic call. FROZEN: examples/add manual struct add->{sum:42} + modal_registry (live-suite backbone), runner protocol, 5 error kinds, wire format.',
  phases: [
    { title: 'Design', detail: 'read the macro example + every consumer (live_auto_io/orchestrate/live_secrets_volumes/local.rs) + README; produce the exact edit/rename/relocate list to examples-readme-ergonomics-spec.md' },
    { title: 'Implement', detail: 'rename add_plain->add as the headline, slim add-macro to user-style code, relocate proof/config bits, rewire consumers + README; offline gates green + --describe lists `add` (HARD GATE)' },
    { title: 'Review', detail: 'parallel: (1) add reads like a clean 3-line Python-ish fn + README matches; (2) nothing broke — gates green, frozen infra intact' },
  ],
}

const ROOT = '/Users/nicolas/devel/modal-rust'

const SHARED = [
  'Repo root: ' + ROOT + ' (git on main). GOAL (user ask): the examples must SHOWCASE the macro ergonomics and "read like a',
  'final user would write it — using macro ergonomics to make it look like the python funcs. Something like `add` should',
  'be just a few lines of code." It is fine to have one macro example and one manual example; we want at least one clean',
  'one of each.',
  '',
  '## The problem (verified)',
  'examples/add-macro/src/lib.rs is a 442-line proof/reference file with FIVE entrypoints (add[struct], add_plain[ergonomic],',
  'add_gpu, add_extras, secret_vol_probe) + a ~260-line #[cfg(test)] module + heavy doc-comments. Its HEADLINE `add` is the',
  'VERBOSE struct form (`fn add(input: AddInput) -> Result<AddOutput>` with hand-written AddInput/AddOutput), while the clean',
  'ergonomic auto-I/O form is a SECONDARY function awkwardly named `add_plain` (`fn add_plain(a:i64,b:i64)->Result<i64>`).',
  'The user wants the ergonomic 3-liner to BE `add`. This does not read like user code.',
  '',
  '## The macro feature it should show (already shipped)',
  '`#[modal_rust::function]` on a PLAIN signature `fn add(a: i64, b: i64) -> anyhow::Result<i64>` auto-generates a named',
  '`add::Input`/`Output`, registers a spread shim via the frozen `typed!`, and adds a typed `App` method via a generated',
  '`AddCall` trait so `app.add(2, 3).local()?/.remote().await?/.spawn()/.map(..)` works with NO type ever named (the explicit',
  '`app.function("add").remote(add::Input{a,b})` path also still works). A macro-using crate needs ONLY the `modal-rust`',
  'facade dep (see examples/add-macro/Cargo.toml).',
  '',
  '## EXACT coupling map (already traced — do not re-derive from scratch, but VERIFY before editing)',
  '- examples/add (MANUAL, FROZEN INFRA — do NOT change its struct `add` semantics or registration): defines struct',
  '  `add(AddInput)->AddOutput` returning {sum} (=> {sum:42} for {40,2}), plus error-kind entrypoints fail/fail_structured/',
  '  bad_encode/will_panic, and `modal_registry()`. This struct `add`->{sum:42} is the BACKBONE of the live suite:',
  '  live_deploy.rs, live_cache.rs, live_remote.rs and examples/orchestrate ALL import `example_add::{modal_registry,',
  '  AddInput, AddOutput}` and assert `.function("add")...{sum:42}`. crates/.../tests/local.rs (offline) drives the error',
  '  kinds. => Keep examples/add\'s struct `add`, AddInput/AddOutput, error-kind entrypoints, and modal_registry REGISTRATION',
  '  behavior intact. (You MAY tidy its comments / relocate the error-kind FN BODIES into a `pub mod error_kinds;` so the',
  '  headline reads cleaner — ONLY IF modal_registry still registers them and local.rs stays green; if risky, leave',
  '  examples/add as-is. The manual example already reads acceptably as hand-written code; the macro example is the priority.)',
  '- examples/add-macro (MACRO — the thing to clean): externally-coupled symbols are `add_plain` + `AddPlainCall` (used by',
  '  live_auto_io.rs and examples/orchestrate) and `secret_vol_probe`+ProbeInput/ProbeOutput (used by live_secrets_volumes.rs).',
  '  Its struct `add`, `add_gpu`, `add_extras` are used ONLY by add-macro\'s own #[cfg(test)] module (NOT by any external',
  '  test). Cargo.toml already has the single `modal_rust_facade` dep + serde/anyhow.',
  '',
  '## The change to implement',
  '1. **Promote the ergonomic form to be the headline `add`.** In examples/add-macro: RENAME `add_plain` -> `add`',
  '   (`#[modal_rust::function] pub fn add(a: i64, b: i64) -> anyhow::Result<i64> { Ok(a + b) }`) and DELETE the old',
  '   struct-form `add` (and its AddInput/AddOutput) from this crate. The generated artifacts rename accordingly:',
  '   `add::Input/Output`, trait `AddCall`, method `app.add(2, 3)`. The showcased src/lib.rs should become SHORT and',
  '   user-style: a 2-3 line module doc, `extern crate modal_rust_facade as modal_rust;`, `use modal_rust::function;`, and',
  '   the ~3-line `add`. Aim for the whole face of lib.rs to read like the Python `@app.function() def add(a,b): return a+b`.',
  '2. **Relocate the non-headline bits OUT of the showcased lib.rs** (keep them compiling; they are needed):',
  '   - Move `secret_vol_probe` + ProbeInput + ProbeOutput into `examples/add-macro/src/proof.rs` exposed via',
  '     `pub mod proof;` (one line in lib.rs). Update live_secrets_volumes.rs to the new path if it imports them.',
  '   - Move the per-function CONFIG demo (gpu/timeout/cache/secrets/volumes decorators -> FunctionConfig) coverage into',
  '     `examples/add-macro/tests/config.rs` with tiny decorated fns (preserve the assertions that the decorator parses into',
  '     the inventory Registration), OR drop add_gpu/add_extras entirely if equivalent coverage already exists in',
  '     crates/modal-rust-macros tests (CHECK; prefer keeping coverage in a tests/ file over losing it). Either way they',
  '     leave the showcased lib.rs.',
  '   - Move the remaining useful unit tests into `examples/add-macro/tests/behavior.rs`, slimmed to the essentials for the',
  '     new `add`: direct `add(2,3)?==5`; registered via inventory; `app.add(2,3).local()?==5` (typed method, no type named);',
  '     explicit `app.function("add").local(add::Input{a:2,b:3})?==5`; runner envelope for `--entrypoint add --input-json',
  '     {"a":2,"b":3}` is exactly `{"ok":true,"value":5}`; an unknown_entrypoint error-kind check. Drop the old',
  '     "byte-identical to the manual struct add ({sum:42})" assertion (it compared a now-removed struct fn); the frozen',
  '     runner protocol is already covered in runtime/local.rs.',
  '3. **Rewire consumers** (rename add_plain->add, AddPlainCall->AddCall, app.add_plain->app.add, and fn/test/doc names):',
  '   - crates/modal-rust/tests/live_auto_io.rs (it imports example_add_macro::{add_plain, AddPlainCall}; asserts',
  '     app.add_plain(2,3).remote()==5 and app.function("add_plain")...). Keep its MODAL_RUST_PACKAGE=example-add-macro +',
  '     #[ignore]+#[cfg(feature="live")]; just rename the symbol to `add`.',
  '   - examples/orchestrate/src/main.rs (imports example_add_macro::AddPlainCall; calls macro_app.add_plain(2,3).local();',
  '     unit test local_macro_add_plain_returns_5). Rename to `add`/`AddCall`/`app.add(2,3)`. KEEP the manual',
  '     example_add path (App::new(modal_registry()) + .function("add") struct {sum:42}) untouched — orchestrate shows BOTH.',
  '   - crates/modal-rust/tests/live_secrets_volumes.rs only if it imports the moved proof symbols.',
  '4. **README** (it is PARTLY updated — the macro tutorial at ~line 104 already leads with `#[function] fn add(a,b)` +',
  '   `app.add(2,3).local()/.remote()`; KEEP that): replace every remaining `add_plain` with `add` (the quickstart snippet',
  '   ~line 489-498, the Examples table row for add-macro ~line 480, and any GPU/secrets snippets). Reword the Examples',
  '   table: examples/add = "(manual / no-macro) `add` written by hand — the input struct, the typed! registration, and',
  '   modal_registry — i.e. everything the macro generates for you"; examples/add-macro = "(macro) the SAME `add` in three',
  '   lines: `#[modal_rust::function] fn add(a, b) -> anyhow::Result<i64>`, called `app.add(2, 3).remote().await?`". Ensure',
  '   nothing in the README still references `add_plain`.',
  '',
  '## FROZEN — do NOT change',
  '- examples/add’s struct `add`(AddInput->AddOutput {sum}) semantics + AddInput/AddOutput + error-kind entrypoints +',
  '  modal_registry registration (the live_deploy/cache/remote + orchestrate backbone). The runner CLI protocol, the 5 error',
  '  kinds, the FILE-mode wire, `typed!` behavior, Registry::from_inventory dispatch, and FunctionConfig decorator semantics.',
  '  Do NOT edit crates/modal-rust-runtime, the macro crate, or the SDK invoke logic. cuda-vector-add / burn-add unchanged.',
  '  Do NOT touch the user’s uncommitted docs/testing-strategy.md.',
  '',
  '## Ground-truth refs (READ before editing)',
  '- examples/add-macro/{src/lib.rs,src/bin/modal_runner.rs,Cargo.toml}; examples/add/{src/lib.rs,src/bin/modal_runner.rs};',
  '  examples/orchestrate/src/main.rs; crates/modal-rust/tests/{live_auto_io.rs,live_secrets_volumes.rs,local.rs}; README.md',
  '  (the macro tutorial ~104-136, the Examples table ~473-498, GPU ~408, Secrets ~433).',
  '',
  '## Verification (offline = HARD gate)',
  '- default-members: cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test — all green.',
  '  No file still references `add_plain` / `AddPlainCall` (grep examples/ crates/ README.md — must be ZERO).',
  '- `cargo run -q -p example-add-macro --bin modal_runner -- --describe` lists entrypoint `add` (and the proof entrypoint),',
  '  and `--entrypoint add --input-json {"a":2,"b":3}` prints exactly `{"ok":true,"value":5}`. Paste both.',
  '- Show the FULL new examples/add-macro/src/lib.rs (it should be short — quote it) to prove it reads like user code.',
  '- Live (best-effort, cheap CPU remote — NOT GPU; never block on Modal): live_auto_io proving app.add(2,3).remote()==5.',
  '',
  '## How to return',
  'End with: "RESULT: <STATUS> — <one-line>". Build phases STATUS in {BUILD_GREEN, BUILD_FAILED} + exact cargo output + the',
  '--describe/run proof + the quoted new lib.rs + confirmation zero `add_plain` references remain and examples/add infra is intact.',
].join('\n')

phase('Design')
const spec = await agent(SHARED + '\n\n' + [
  'YOUR TASK (Design). READ all the ground-truth refs. VERIFY the coupling map (esp. exactly what live_auto_io.rs,',
  'orchestrate, live_secrets_volumes.rs, and local.rs import/assert, and whether crates/modal-rust-macros already covers the',
  'decorator-config parsing so add_gpu/add_extras can be dropped vs moved to tests/config.rs). WRITE a build-ready,',
  'file-by-file edit list to ' + ROOT + '/workpads/shim-backend/examples-readme-ergonomics-spec.md: the exact rename',
  '(add_plain->add, AddPlainCall->AddCall), the deletion of the struct add from add-macro, the new minimal lib.rs (quote it',
  'verbatim), the proof.rs + tests/{behavior.rs,config.rs} relocations, every consumer rewrite (live_auto_io, orchestrate,',
  'live_secrets_volumes) with line refs, the README edits (every add_plain occurrence + the table rewording), and an',
  'explicit note on whether examples/add gets the optional error_kinds module tidy or is left as-is (favor SAFE: leave it if',
  'local.rs coupling is non-trivial). Flag any risk. RESULT: SPEC_DONE — wrote examples-readme-ergonomics-spec.md',
].join('\n'), { phase: 'Design', label: 'design:examples-readme' })

phase('Implement')
const impl = await agent(SHARED + '\n\n' + [
  'The spec is at ' + ROOT + '/workpads/shim-backend/examples-readme-ergonomics-spec.md — READ IT FIRST.',
  '',
  'YOUR TASK (Implement — HARD GATE). Execute the spec: make the ergonomic `add(a,b)->Result<i64>` the headline of',
  'examples/add-macro (rename add_plain->add, delete the struct add, slim lib.rs to user-style code), relocate proof/config/',
  'test bits, rewire live_auto_io + orchestrate + live_secrets_volumes, and update the README so `add` (never `add_plain`) is',
  'the ergonomic call. Keep examples/add’s struct-add infra FROZEN. Do NOT touch crates/modal-rust-runtime, the macro crate,',
  'the SDK invoke logic, or docs/testing-strategy.md.',
  'VERIFY (offline HARD, paste exact output): cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ;',
  'cargo test (default-members) — all green. grep -rnE "add_plain|AddPlainCall" examples/ crates/ README.md => MUST be empty.',
  '`cargo run -q -p example-add-macro --bin modal_runner -- --describe` (lists `add`) and `... -- --entrypoint add',
  '--input-json {"a":2,"b":3}` (=> {"ok":true,"value":5}). QUOTE the full new examples/add-macro/src/lib.rs. Best-effort',
  'cheap live: live_auto_io app.add(2,3).remote()==5 (CPU; never block on Modal).',
  'RESULT: BUILD_GREEN — ergonomic `add` is the headline; user-style example; gates green; zero add_plain refs   (or BUILD_FAILED — <reason>)',
].join('\n'), { phase: 'Implement', label: 'examples-readme-impl' })

const implGreen = /RESULT:\s*BUILD_GREEN/i.test(impl || '')
if (!implGreen) log('Implement HARD GATE not green — see Review for the blocker.')

phase('Review')
const reviews = await parallel([
  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / reads-like-user-code + README). Against the code: QUOTE the full new examples/add-macro/src/lib.rs',
    'and judge — is `add` a ~3-line plain-signature `#[function] fn add(a,b)->Result<i64>` with NO hand-written I/O structs,',
    'and does the file read like what a real user writes (no 400-line proof dump in the face of it)? Confirm the typed call',
    'is `app.add(2,3)` (trait AddCall) and the explicit `add::Input` path works. Confirm the README leads with this `add`,',
    'the Examples table contrasts manual-by-hand vs the 3-line macro, and grep shows ZERO `add_plain`/`AddPlainCall` left in',
    'examples/ crates/ README.md. RESULT: PASS — clean user-style `add`, README matches  (or FAIL — <what is wrong>)',
  ].join('\n'), { phase: 'Review', label: 'review:reads-clean' }),

  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / nothing broke — RUN the gates). From ' + ROOT + ' report exact output + exit status: cargo',
    'fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test (default-members). Confirm the',
    'FROZEN infra is intact: examples/add still has struct `add`(AddInput->AddOutput {sum}) + error kinds + modal_registry,',
    'and live_deploy/live_cache/live_remote/orchestrate still compile against it (cargo build -p example-orchestrate;',
    'cargo test -p modal-rust --features live --no-run). Confirm `example-add-macro --describe` lists `add` and the run',
    'prints `{"ok":true,"value":5}`. Report any failure verbatim. RESULT: PASS — gates green, frozen infra intact  (or FAIL — <exact failing cmd+output>)',
  ].join('\n'), { phase: 'Review', label: 'review:gates' }),
])

return { impl_green: implGreen, impl, reviews }
