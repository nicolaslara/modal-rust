export const meta = {
  name: 'examples-use-macro',
  description: 'Make the examples SHOWCASE the macro ergonomics: keep examples/add as the canonical MANUAL reference (byte-identical), and convert the GPU examples (burn-add, cuda-vector-add) to the self-describing #[modal_rust::function(gpu=..)] decorator form (auto-I/O + decorator-is-config) so the ergonomics are visible. Keep examples/add-macro the macro twin. Adapt the live tests (live_burn, live_gpu) to stay green. Frozen: runner protocol / typed! / 5 error kinds / decorator semantics / macro==manual equivalence. Runs AFTER macro-auto-io + macro-hygiene (both committed).',
  phases: [
    { title: 'Design', detail: 'read the post-hygiene macro + every example + the live-test local-create inventory split; spec a behavior-preserving conversion (which examples become macro, how the live tests adapt) to examples-use-macro-spec.md' },
    { title: 'Implement', detail: 'convert burn-add + cuda-vector-add to the decorator form; keep examples/add manual + byte-identical + add-macro as the twin; adapt orchestrate + live tests; update README Examples section; offline gates green + --describe shows the gpu config (HARD GATE)' },
    { title: 'Review', detail: 'parallel: ergonomics visible + manual reference preserved + frozen invariants held + gates green' },
  ],
}

const ROOT = '/Users/nicolas/devel/modal-rust'

const SHARED = [
  'You are making the modal-rust EXAMPLES showcase the macro ergonomics (repo root: ' + ROOT + '; git on main).',
  'Two macro features JUST landed and are committed: (a) auto-I/O — `#[modal_rust::function]` on a PLAIN signature',
  '`fn add(a:i64,b:i64)->i64` auto-generates a named `add::Input`/`Output` + a typed `App` extension method',
  '`app.add(2,3).local()/.remote()/.spawn()/.map()` (and the explicit `add::Input{..}` path still works); an explicit',
  'single-user-struct param stays byte-identical to the manual `typed!(fn)`. (b) hygiene — the macro routes every emitted',
  'path through the `modal-rust` facade `__private`, so a macro-using crate needs ONLY `modal-rust` (+ serde/anyhow), NOT',
  '`modal-rust-runtime`/`inventory` as direct deps (see examples/add-macro/Cargo.toml as the template).',
  '',
  '## The goal (user ask)',
  '"examples don\'t use the macros. We want them to use the macros (we can have SOME examples explicitly NOT use the macros',
  'to teach the no-macro path, maybe two versions of each), but we should be able to SEE the ergonomics in the examples."',
  '',
  '## The examples landscape (READ each before changing)',
  '- examples/add — the CANONICAL MANUAL reference: hand-written `modal_registry()` + `typed!(add)`, explicit `AddInput`/',
  '  `AddOutput`. KEEP IT MANUAL AND BYTE-IDENTICAL — it is the teaching example for the no-macro path AND the anchor of',
  '  the `macro_path_byte_identical_to_manual` equivalence proof (in examples/add-macro). DO NOT TOUCH examples/add.',
  '- examples/add-macro — the MACRO TWIN of examples/add: already uses `#[modal_rust::function]` (explicit `add` + auto-I/O',
  '  `add_plain`). This is the ergonomic showcase for the basic case. Behavior must not change; you MAY only improve its',
  '  doc-comments to make the ergonomic surface (auto-I/O plain-sig + typed `app.add_plain(2,3)` + the named-input path)',
  '  more obvious. No code/semantics change.',
  '- examples/burn-add — GPU (burn ML framework). Currently MANUAL: `Registry::new().function("burn_add", typed!(burn_add))`,',
  '  no decorator config; GPU is injected CALLER-SIDE (live_burn.rs). EXCLUDED from default-members (heavy / CUDA).',
  '- examples/cuda-vector-add — GPU (cudarc dynamic-load, in default-members). Currently MANUAL: `Registry::new().',
  '  function("vector_add", typed!(vector_add))`; GPU injected caller-side (live_gpu.rs uses a decorated STUB to capture',
  '  `FunctionConfig{gpu:"T4"}` while uploading MODAL_RUST_PACKAGE=example-cuda-vector-add and building the REAL crate remotely).',
  '- examples/orchestrate — a DRIVER binary importing `example_add::{modal_registry,AddInput,AddOutput}`; drives `.local()`,',
  '  `.remote()`, and deploy+call. Its doc already mentions the `App::from_inventory()` macro alternative.',
  '',
  '## The conversion to implement (behavior-preserving)',
  '1. **burn-add + cuda-vector-add → self-describing decorator form.** Put `#[modal_rust::function(gpu = "<the gpu these',
  '   already target>", name = "<existing entrypoint name>")]` DIRECTLY on the real `burn_add` / `vector_add` fn (read',
  '   live_burn.rs / live_gpu.rs for the exact gpu string + entrypoint name they expect — preserve them EXACTLY). Switch',
  '   the runner bin from `run_cli(<crate>::modal_registry())` to the inventory path (`App`-free: the runner uses',
  '   `modal_rust::__private::runtime::Registry::from_inventory()` or the facade re-export — mirror examples/add-macro/src/',
  '   bin/modal_runner.rs EXACTLY). Drop the now-unnecessary direct `modal-rust-runtime` + `inventory` deps from their',
  '   Cargo.toml (single `modal-rust` facade dep + serde/anyhow, exactly like examples/add-macro). The function BODY (the',
  '   real GPU compute) and its input/output types are UNCHANGED; if the fn already takes a single user-struct param it',
  '   stays the explicit form (byte-identical), only the decorator + registration mechanism changes. If you keep a',
  '   hand-written `modal_registry()` for back-compat, it must NOT double-register with the inventory path — pick ONE',
  '   registration mechanism (prefer inventory, like add-macro) and update the runner + any in-crate tests accordingly.',
  '2. **Adapt the live tests (live_burn.rs, live_gpu.rs) — keep them COMPILING + their live proof intact.** The subtlety:',
  '   the OUTBOUND FunctionCreate is built from the TEST binary\'s LOCAL `App::from_inventory()` BEFORE the remote build, so',
  '   the gpu `FunctionConfig` must be known LOCALLY at create time. Today each test declares a decorated STUB in the test',
  '   binary to supply that. After the conversion, decide (and DO) the cleanest correct thing: either (a) the test depends',
  '   on the converted example crate so its decorated real fn registers into the linked inventory and the stub is dropped,',
  '   or (b) the stub stays (now redundant with the crate\'s own decorator) — whichever keeps `app.function("<name>").',
  '   remote()` producing the SAME outbound gpu_config. Do NOT weaken the server-side proof (the decoded gpu_name). The',
  '   tests stay `#[ignore]` + `#[cfg(feature="live")]`; they must COMPILE under `--features live` and pass offline-compiled.',
  '3. **orchestrate — show the macro path too.** Add (or activate) an `App::from_inventory()` demonstration alongside the',
  '   existing explicit `App::new(modal_registry())` path, so the driver visibly exercises BOTH the manual registry and the',
  '   macro/inventory ergonomics. Keep the explicit path (it teaches the no-macro driver usage). If orchestrate imports',
  '   from example_add (manual), you may additionally import the macro twin to show `app.add_plain(2,3)` typed-method',
  '   ergonomics. Keep the default offline `.local()` path working with zero Modal.',
  '4. **README Examples section.** Update it to label each example macro-vs-manual and show the ERGONOMIC invocation',
  '   (`app.add_plain(2,3).remote().await?`, `#[function(gpu="T4")]`). Keep "how to run" accurate. Do NOT touch the',
  '   user\'s uncommitted docs/testing-strategy.md.',
  '',
  '## FROZEN invariants — do NOT change',
  '- examples/add stays MANUAL and BYTE-IDENTICAL (the no-macro teaching reference). The `macro_path_byte_identical_to_manual`',
  '  equivalence test must still pass. Runner CLI protocol / `typed!` behavior / `Registry::from_inventory` dispatch / the 5',
  '  error kinds / FILE-mode wire / decorator semantics (gpu/timeout/cache/secrets/volumes) — UNCHANGED. The real GPU compute',
  '  bodies + their input/output types are unchanged. This is an example-surface + registration-mechanism change, not a',
  '  protocol or runtime change. Do NOT edit crates/modal-rust-runtime, the SDK invoke logic, or the macro crate.',
  '',
  '## Ground-truth references (READ the CURRENT, post-hygiene sources)',
  '- examples/add-macro/{Cargo.toml,src/lib.rs,src/bin/modal_runner.rs} — the TEMPLATE for the converted form (single',
  '  modal-rust dep; inventory runner; auto-I/O + decorator usage).',
  '- examples/add/{Cargo.toml,src/lib.rs} — the manual reference (do NOT change; mirror its types/body in the converted crates).',
  '- examples/burn-add/* , examples/cuda-vector-add/* , examples/orchestrate/* — the crates to change.',
  '- crates/modal-rust/tests/live_burn.rs , crates/modal-rust/tests/live_gpu.rs — the live tests to adapt (gpu string +',
  '  entrypoint name + the local-create inventory split).',
  '- crates/modal-rust-macros/src/lib.rs (classifier + emit, for reference only), crates/modal-rust/src/deploy.rs +',
  '  remote.rs (how `config.gpu` -> `with_gpu` -> Resources.gpu_config), README.md (Examples section).',
  '',
  '## Verification rules (WORKING.md) — offline is the HARD gate',
  '- default-members gates all green: `cargo fmt --check` ; `cargo clippy --all-targets -- -D warnings` ; `cargo build` ;',
  '  `cargo test` — INCLUDING `macro_path_byte_identical_to_manual`.',
  '- cuda-vector-add is in default-members (cudarc dynamic-loads; builds with no CUDA). burn-add is EXCLUDED — build it',
  '  explicitly: `cargo build -p example-burn-add` if it compiles here without a CUDA toolchain; if it genuinely needs CUDA',
  '  to build, fall back to `cargo check -p example-burn-add` (or document precisely why even check fails) — do NOT let an',
  '  un-buildable burn-add block the rest; note the limitation.',
  '- ERGONOMICS PROOF (offline, REQUIRED for each converted example): run its `modal_runner --describe` and confirm the',
  '  entrypoint config now reports the gpu (e.g. gpu "T4") that the decorator declares — proving decorator-is-config rides',
  '  through inventory. Paste the --describe JSON.',
  '- Live GPU re-run is BEST-EFFORT, not required: the gpu wire path is already live-proven by live_gpu/live_burn. If Modal',
  '  credentials are present, a CHEAP T4 ephemeral run of the converted cuda-vector-add via live_gpu is a nice bonus (per the',
  '  AFK note: cheap T4, ephemeral apps, never commit tokens). Do NOT run the heavy burn build live. Never block on Modal.',
  '',
  '## How to return',
  'End with: "RESULT: <STATUS> — <one-line>". Build phases STATUS in {BUILD_GREEN, BUILD_FAILED} + exact cargo output +',
  'the --describe gpu-config proof for each converted example + confirmation examples/add is byte-identical and the',
  'equivalence test passes.',
].join('\n')

phase('Design')
const spec = await agent(SHARED + '\n\n' + [
  'YOUR TASK (Design the conversion). READ: examples/add-macro/* (the template), examples/add/* (the untouched manual',
  'reference), examples/burn-add/* , examples/cuda-vector-add/* , examples/orchestrate/* , crates/modal-rust/tests/',
  'live_burn.rs + live_gpu.rs (extract the EXACT gpu string + entrypoint name each expects + how they capture config',
  'locally), and the README Examples section. WRITE a build-ready spec to ' + ROOT + '/workpads/shim-backend/',
  'examples-use-macro-spec.md covering, with file:line citations: (1) the precise decorator to add to burn_add /',
  'vector_add (gpu + name, copied from the live tests) and the runner-bin rewrite to the inventory path (mirror',
  'add-macro); (2) the exact Cargo.toml dep removals for each (single modal-rust facade dep); (3) the registration',
  'mechanism decision (inventory vs keep modal_registry — exactly one, no double-register) and what in-crate tests need',
  'updating; (4) THE LIVE-TEST ADAPTATION: resolve the local-create inventory split — does the converted crate\'s decorator',
  'let the test drop its stub (test depends on the crate) or does the stub stay? Specify the concrete edit for live_burn',
  'AND live_gpu that keeps them compiling under --features live and preserves the outbound gpu_config + the server-side',
  'proof; (5) the orchestrate change (add an App::from_inventory() / typed app.fn() demonstration, keep the explicit path',
  'and the zero-Modal .local() default); (6) the README Examples-section edits. Flag any risk that burn-add cannot build',
  'locally without CUDA (and the check-only fallback). RESULT: SPEC_DONE — wrote examples-use-macro-spec.md',
].join('\n'), { phase: 'Design', label: 'design:examples' })

phase('Implement')
const impl = await agent(SHARED + '\n\n' + [
  'RESUME NOTE (attempt 2): the previous run DIED on a transient socket error BEFORE applying ANY edits — the working tree',
  'is still fully UNCONVERTED (burn-add + cuda-vector-add are untouched manual crates; orchestrate + README unchanged). The',
  'Design spec was written successfully. Start the conversion FRESH from the spec now; do not assume any prior partial work.',
  '',
  'The spec is at ' + ROOT + '/workpads/shim-backend/examples-use-macro-spec.md — READ IT FIRST.',
  '',
  'YOUR TASK (Implement the conversion — HARD GATE). Per the spec: convert burn-add + cuda-vector-add to the',
  'self-describing `#[modal_rust::function(gpu=..)]` decorator form with the inventory runner + single modal-rust dep',
  '(mirror examples/add-macro EXACTLY); leave examples/add MANUAL + byte-identical; keep examples/add-macro the twin',
  '(doc-only polish allowed, no behavior change); adapt live_burn.rs + live_gpu.rs so they still compile under',
  '--features live and preserve the outbound gpu_config + server-side proof; update orchestrate to demonstrate the',
  'macro/inventory path alongside the explicit one (default .local() stays zero-Modal); update the README Examples',
  'section. Do NOT touch crates/modal-rust-runtime, the macro crate, the SDK invoke logic, or docs/testing-strategy.md.',
  '',
  'VERIFY (offline HARD): from ' + ROOT + ' run and paste exact output + exit status:',
  '- cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test  (default-members) — all',
  '  green, INCLUDING macro_path_byte_identical_to_manual.',
  '- cargo build -p example-cuda-vector-add (default set). For burn: cargo build -p example-burn-add, or cargo check -p',
  '  example-burn-add with the documented reason if a full build needs CUDA.',
  '- ERGONOMICS PROOF: for EACH converted example, build its runner and run `modal_runner --describe`; paste the JSON and',
  '  point at the entrypoint whose config now shows the decorator gpu (e.g. "T4"). Also confirm `git diff --stat',
  'examples/add` is EMPTY (byte-identical manual reference).',
  '- Live: ONLY if Modal credentials are present and cheap — a single T4 ephemeral live_gpu run of converted',
  '  cuda-vector-add is a bonus; never block on Modal; do NOT run burn live.',
  'RESULT: BUILD_GREEN — examples showcase macro ergonomics; add untouched; gates + --describe gpu-proof green   (or BUILD_FAILED — <reason>)',
].join('\n'), { phase: 'Implement', label: 'examples-impl' })

const implGreen = /RESULT:\s*BUILD_GREEN/i.test(impl || '')
if (!implGreen) log('Implement HARD GATE not green — example conversion failed to compile / broke a gate. Review documents the blocker.')

phase('Review')
const reviews = await parallel([
  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / ergonomics visible + manual reference preserved). Verify against the code:',
    '- examples/add is UNCHANGED (git diff --stat examples/add empty) and still the manual `modal_registry()` + `typed!`',
    '  reference; `macro_path_byte_identical_to_manual` passes.',
    '- burn-add + cuda-vector-add now carry `#[modal_rust::function(gpu=..)]` on the real fn, use the inventory runner, and',
    '  their Cargo.toml lists ONLY modal-rust (no modal-rust-runtime / inventory). Quote each trimmed Cargo.toml + the',
    '  decorated signature. Their `--describe` reports the gpu config (decorator-is-config rode through). The GPU compute',
    '  body + I/O types are unchanged.',
    '- live_burn + live_gpu still compile under --features live and preserve the outbound gpu_config + server-side proof',
    '  (no weakened assertion). orchestrate demonstrates the macro/inventory path AND keeps the explicit path + zero-Modal',
    '  .local() default. README Examples section labels macro-vs-manual + shows the ergonomic invocation.',
    'RESULT: PASS — ergonomics visible, manual reference + frozen invariants preserved  (or FAIL — <what is wrong>)',
  ].join('\n'), { phase: 'Review', label: 'review:ergonomics' }),

  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / gates — RUN them). From ' + ROOT + ' report exact output + exit status:',
    '- cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test  (default-members).',
    '- cargo build -p example-cuda-vector-add ; and cargo build (or check) -p example-burn-add — report which + why.',
    'Confirm example-burn-add still EXCLUDED from default-members; no hand-written file grossly exceeds ~500 LOC; the',
    'converted runner bins match the add-macro inventory pattern. Report any failure verbatim.',
    'RESULT: PASS — gates green  (or FAIL — <exact failing command + output>)',
  ].join('\n'), { phase: 'Review', label: 'review:gates' }),
])

return { impl_green: implGreen, impl, reviews }
