export const meta = {
  name: 'mock-backend-port',
  description: 'Port the Modal test mock backend to a rust-native in-process gRPC mock so we can write CLEAN Rust tests (table + regular) of the remote/gRPC path with NO real Modal and NO Python. Stand up an in-process tonic ModalClient server on loopback, point the SDK at it via the existing from_config(server_url) injection, record requests + let tests configure responses ergonomically, and PROVE it with an example test exercising a main feature end-to-end. First cut: the ~17 RPCs the SDK actually calls (happy-path defaults + per-test overrides). FROZEN: SDK client behavior, runner protocol, facade public API.',
  phases: [
    { title: 'Design', detail: 'read the SDK gRPC surface + from_config/channel + facade connect + docs/testing-strategy.md; SPIKE a minimal loopback tonic ModalClient mock the SDK client can dial; spec the crate layout, the 201-vs-17 server-gen approach, the record+config API, and table+regular ergonomics' },
    { title: 'Implement', detail: 'build crates/modal-rust-testkit (in-process mock + record + ergonomic response config) + an example test proving a main feature end-to-end against the mock; gates green (HARD GATE)' },
    { title: 'Review', detail: 'parallel: (1) API is rust-like + supports table AND regular tests + the example proves a real feature; (2) gates green + SDK/runner/facade behavior unchanged (additive only)' },
  ],
}

const ROOT = '/Users/nicolas/devel/modal-rust'

const SHARED = [
  'Repo root: ' + ROOT + ' (git on main). GOAL (user ask): "start porting the test mock backend. Make it RUST-LIKE so we can',
  'write clean Rust tests on our end. This may be table tests or regular tests (ideally support BOTH). Ensure the mock works',
  'by adding an example test for some of our main features." This is the "build our own" recommendation from the just-',
  'completed mock-reuse investigation (see docs/testing-strategy.md): the Python MockClientServicer binds a real loopback',
  'grpclib server but ALL per-test behavior is live Python state set in-process — so we MIRROR THE PATTERN natively in Rust',
  'rather than reuse the Python artifact.',
  '',
  '## What to build (a rust-native, in-process gRPC mock backend)',
  'An in-process tonic server implementing the `ModalClient` gRPC service, started on a loopback port (tokio), that:',
  '  (1) RECORDS every request it receives (typed + queryable from the test, e.g. `mock.requests::<FunctionCreateRequest>()`',
  '      or `mock.last::<FunctionCreateRequest>()`), and',
  '  (2) returns RESPONSES that tests configure ERGONOMICALLY — sensible happy-path DEFAULTS for the RPCs a basic deploy/',
  '      call/remote flow needs, plus per-test OVERRIDES via builder + closures (e.g. `.on_function_get_outputs(|req| ...)`,',
  '      `.function_result(<canned output>)`), and',
  '  (3) is pointed-to by the SDK with NO transport change: the SDK channel already dials plain `http://` (channel.rs:16-31',
  '      "so a local dev server works") and `ModalClient::from_config(ModalConfig{ server_url, .. })` injects an arbitrary',
  '      URL (client.rs ~63-74). Tests get the mock\'s `http://127.0.0.1:<port>` and feed it to the SDK / facade.',
  '',
  '## The KEY technical unknown — DE-RISK IT IN DESIGN WITH A REAL SPIKE',
  'The `ModalClient` proto service has 201 RPCs (crates/modal-rust-sdk/proto/api.proto:4129) but the SDK only calls ~17:',
  'AppCreate, AppGetOrCreate, AppPublish, BlobCreate, EnvironmentGetOrCreate, FunctionCreate, FunctionPrecreate,',
  'FunctionGet, FunctionMap, FunctionPutInputs, FunctionGetOutputs, ImageGetOrCreate, ImageJoinStreaming, MountGetOrCreate,',
  'MountPutFile, SecretGetOrCreate, VolumeGetOrCreate. The SDK build.rs is client-only (build_server(false)). A tonic',
  '`build_server(true)` generates a service trait with ALL 201 methods, every one REQUIRED to implement. Resolve the',
  'cleanest path and PROVE it compiles + the SDK client can dial it:',
  '  - Option A: testkit crate runs its OWN build.rs `build_server(true)` on the same api.proto, and provides the 201-method',
  '    trait impl where the ~17 used RPCs dispatch to recording/handlers and the other ~184 return `Status::unimplemented`',
  '    via a generated/macro stub (write a `mock_unimplemented!` macro or a small codegen so it is not 184 hand-written fns).',
  '    The testkit generates its OWN message types from the proto; that is FINE — wire-compatible with the SDK\'s client',
  '    types, and test assertions/response-construction use the testkit types.',
  '  - Option B: a trimmed `.proto` mock service with only the ~17 rpcs reusing the message types — evaluate if tonic makes',
  '    this clean (it may force regenerating all messages anyway).',
  '  Pick the one that actually compiles with the least boilerplate. THE SPIKE (do it in Design): generate the server for',
  '  ONE rpc (e.g. FunctionGet or AppGetOrCreate), stand it up on a loopback port in a throwaway test, point a real',
  '  `ModalClient::from_config` SDK client at it, call that one RPC, and assert the round-trip + that the request was',
  '  recorded. Only spec the full thing after this round-trips.',
  '',
  '## Ergonomics (rust-like; support BOTH table AND regular tests)',
  'Design a clean API. Sketch (not prescriptive): a builder `MockModal::builder()....start().await? -> MockModal` returning',
  'a handle that owns the server task + a shared request log; `mock.url()` for the `http://` addr; typed accessors like',
  '`mock.requests::<FunctionCreateRequest>()` / `mock.took::<FunctionCreateRequest>()`. Response config via chained setters',
  'or closures. It MUST be ergonomic in a TABLE test (the mock + its config is a value you build per case in a loop over',
  'cases) AND in a plain `#[tokio::test]`. Show BOTH styles in the example tests. Tear down cleanly on drop. Deterministic',
  'ids/responses (no Date/random). Consider a tiny prelude for `use modal_rust_testkit::prelude::*;`.',
  '',
  '## The example test (prove the mock works on a MAIN feature, end-to-end)',
  'Drive a REAL modal-rust feature through the facade/SDK against the mock and assert offline — pick at least ONE strong',
  'end-to-end flow, e.g.: deploy + call (App::deploy_with -> the mock captures the FunctionCreate manifest; assert its',
  'name/gpu/timeout/secrets/volumes match the decorator config; App::call -> the mock returns a canned FunctionGetOutputs',
  'result -> assert the facade DECODES it to the typed output). ALSO add a TABLE test (e.g. a table of decorator configs ->',
  'each asserts the captured FunctionCreate fields: gpu None/"T4", timeout, secrets, volumes) to prove table-test ergonomics.',
  'These tests must run with NO Modal credentials and NO network beyond loopback. If wiring a full deploy/call is too deep',
  'for the first cut, fall back to a narrower-but-real flow (e.g. the SDK ops layer: ensure_function builds + sends a',
  'FunctionCreate the mock captures) — but PREFER a facade-level end-to-end and clearly state what is covered (no silent caps).',
  '',
  '## FROZEN — additive only',
  'Do NOT change SDK client behavior, the runner CLI protocol, the 5 error kinds, the FILE-mode wire, the facade PUBLIC API,',
  'or the macro/runtime. The mock is NEW test infrastructure (a new crate crates/modal-rust-testkit, a workspace member;',
  'keep it a dev/test concern — used as a dev-dependency of the facade tests; do not make the shipped crates depend on it).',
  'A SMALL, test-only injection of server_url into the facade is allowed ONLY IF the facade cannot already be pointed at a',
  'custom server via from_config/RemoteConfig/env — check first and prefer the existing path. Do NOT touch the user\'s',
  'uncommitted docs/testing-strategy.md (you may READ it).',
  '',
  '## Ground-truth refs (READ)',
  '- crates/modal-rust-sdk/{build.rs, proto/api.proto (service ModalClient @4129), src/channel.rs, src/client.rs (from_config',
  '  / server_url), src/ops/* (which RPCs + how requests are built)}.',
  '- crates/modal-rust/src/{remote.rs, deploy.rs, scope.rs} + how App::connect/RemoteConfig picks the server URL (for the',
  '  facade-level example test + the server_url injection question).',
  '- docs/testing-strategy.md (the layered plan + the mock-reuse section: Option A in-process tonic mock; Option D RecordingSink).',
  '- existing offline tests for patterns: crates/modal-rust-sdk tests, crates/modal-rust/tests/local.rs.',
  '',
  '## Verification (offline = HARD gate; NO Modal, NO Python)',
  '- default-members + the new testkit crate: cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ;',
  '  cargo test — all green. The example tests (table + regular) PASS offline (loopback only). Paste exact output + the test',
  '  names. QUOTE the example test(s) so the rust-like ergonomics are visible. Confirm the shipped crates do NOT depend on',
  '  the testkit (only dev-dependencies / the testkit crate itself).',
  '',
  '## How to return',
  'End with: "RESULT: <STATUS> — <one-line>". Build phases STATUS in {BUILD_GREEN, BUILD_FAILED} + exact cargo output + the',
  'quoted example test(s) + confirmation the SDK client round-trips against the in-process mock and the request was recorded.',
].join('\n')

phase('Design')
const spec = await agent(SHARED + '\n\n' + [
  'YOUR TASK (Design + SPIKE). Read the ground-truth refs. Then DO THE SPIKE: in a scratch location, generate a tonic',
  'server for the ModalClient service (resolve the 201-method problem — Option A macro-stub vs B trimmed proto), implement',
  'ONE rpc that records + returns a canned response, stand it up on a loopback port, point a real',
  '`modal_rust_sdk::ModalClient::from_config(...)` client at `http://127.0.0.1:<port>`, call that rpc, and CONFIRM the',
  'round-trip + recording works (paste the proof). Decide: can the facade be pointed at a custom server_url via existing',
  'from_config/RemoteConfig/env, or is a tiny test-only injection needed? THEN write a build-ready spec to ' + ROOT +
  '/workpads/shim-backend/mock-backend-spec.md: the new crate layout (crates/modal-rust-testkit: Cargo.toml + build.rs +',
  'the server-gen approach that compiled in the spike), the request-recording design, the ergonomic response-config API',
  '(with concrete signatures), how table AND regular tests use it, the exact ~17 RPCs to implement for the first cut + their',
  'happy-path default responses (enough for a deploy+call / remote flow), the example test plan (one end-to-end facade flow',
  '+ one table test), and the facade server_url injection decision. Cite file:line. RESULT: SPEC_DONE — spike round-tripped; wrote mock-backend-spec.md',
].join('\n'), { phase: 'Design', label: 'design:mock-backend' })

phase('Implement')
const impl = await agent(SHARED + '\n\n' + [
  'RESUME NOTE (attempt 2): the previous Implement DIED on a transient socket error and left',
  'crates/modal-rust-testkit a HALF-BUILT SKELETON. PRESENT on disk: Cargo.toml, build.rs, proto/api.proto, and',
  'src/{macros.rs (mock_unimplemented! for the ~184 unused RPCs), proto.rs (Option-A build_server(true) include),',
  'record.rs (RequestLog + typed requests::<T>()/last::<T>()/took::<T>() accessors), responder.rs (Responses/ResultMode/',
  'OverrideFn/FunctionBody)}. MISSING (so the WHOLE WORKSPACE FAILS TO LOAD — cargo build/test/clippy abort at manifest',
  'load with exit 101 because the member has no lib): src/lib.rs (module wiring `mod macros; mod proto; mod record; mod',
  'responder; mod builder; mod servicer; mod server;` + the prelude + public re-exports + the generated-server include),',
  'src/builder.rs (MockModal::builder() / MockModalBuilder — referenced by responder.rs doc comments but never defined),',
  'src/servicer.rs (the impl ModalClient that dispatches the ~17 used RPCs to recording + per-test responders and',
  'mock_unimplemented!s the rest), src/server.rs (the MockModal handle: start() on a loopback port owning the tokio',
  'server task, url(), shutdown(), Drop teardown), AND ALL example tests (zero exist). COMPLETE the crate from this',
  'half-built state per the spec — do not restart from scratch; keep the good existing files. The FIRST priority is to make',
  'the workspace load again (write src/lib.rs), then finish servicer/server/builder, then the example tests.',
  '',
  'The spec is at ' + ROOT + '/workpads/shim-backend/mock-backend-spec.md — READ IT FIRST (the spike already proved the',
  'server-gen + round-trip approach; follow it).',
  '',
  'YOUR TASK (Implement — HARD GATE). Build crates/modal-rust-testkit per the spec: the in-process tonic ModalClient mock',
  '(the ~17 used RPCs wired to recording + ergonomic per-test response config with happy-path defaults; the ~184 unused',
  'RPCs stubbed via the macro/codegen approach), the `MockModal` builder/handle (start on loopback, `url()`, typed request',
  'accessors, clean teardown, deterministic responses), and a small prelude. Then add the EXAMPLE TESTS proving a main',
  'feature end-to-end against the mock — at least one facade-level flow (deploy+call or remote: assert the captured',
  'FunctionCreate manifest + the facade decoding a canned result) AND one TABLE test (a table of decorator configs ->',
  'assert the captured FunctionCreate fields). Both run offline (loopback only, no Modal/Python). Keep everything additive:',
  'do NOT change SDK client behavior / runner protocol / facade public API (a tiny test-only server_url hook is OK only if',
  'the spec found it necessary). Do not touch docs/testing-strategy.md.',
  'VERIFY (offline HARD, paste exact output): cargo fmt --check ; cargo clippy --all-targets -- -D warnings ; cargo build ;',
  'cargo test (default-members + the testkit crate + the new example tests) — all green. QUOTE the example test(s) verbatim',
  'so the rust-like table+regular ergonomics are visible. Confirm no shipped crate depends on the testkit (dev-dep / member',
  'only). RESULT: BUILD_GREEN — rust-native in-process mock backend + example tests (table+regular) green; SDK round-trips offline   (or BUILD_FAILED — <reason>)',
].join('\n'), { phase: 'Implement', label: 'mock-backend-impl' })

const implGreen = /RESULT:\s*BUILD_GREEN/i.test(impl || '')
if (!implGreen) log('Implement HARD GATE not green — see Review for the blocker.')

phase('Review')
const reviews = await parallel([
  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / ergonomics + proof). QUOTE the example tests and the MockModal public API. Judge: is the API',
    'rust-like and clean (builder + typed request accessors + closure/setter response config), and does it work naturally in',
    'BOTH a table test (mock/config built per case in a loop) AND a plain #[tokio::test]? Does at least one example test',
    'prove a REAL main feature end-to-end (the SDK/facade actually round-trips against the in-process mock over loopback,',
    'the FunctionCreate manifest is captured + asserted, a canned result is decoded) with NO Modal/Python? Confirm it is',
    'pure additive test infra (new crate; shipped crates do not depend on it). RESULT: PASS — rust-like mock, table+regular, real-feature proof  (or FAIL — <what is wrong>)',
  ].join('\n'), { phase: 'Review', label: 'review:ergonomics-proof' }),

  () => agent(SHARED + '\n\n' + [
    'YOUR TASK (Review / gates + frozen — RUN them). From ' + ROOT + ' report exact output + exit status: cargo fmt --check ;',
    'cargo clippy --all-targets -- -D warnings ; cargo build ; cargo test (default-members + testkit). Confirm: the SDK',
    'client behavior, runner protocol, the 5 error kinds, and the facade PUBLIC API are unchanged (additive only; if a',
    'test-only server_url hook was added, it is minimal + test-gated + does not alter normal behavior); the testkit is a',
    'dev/test concern (shipped crates do not depend on it); deterministic (no Date/random); the example tests need no',
    'network beyond loopback + no Modal creds. Report any failure verbatim. RESULT: PASS — gates green, additive, frozen intact  (or FAIL — <exact failing cmd+output>)',
  ].join('\n'), { phase: 'Review', label: 'review:gates-frozen' }),
])

return { impl_green: implGreen, impl, reviews }
